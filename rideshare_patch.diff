--- a/backend/src/migrations/20251006-create-users.js
+++ b/backend/src/migrations/20251006-create-users.js
@@ -0,0 +1,15 @@
+'use strict';
+
+module.exports = {
+  up: async (queryInterface, Sequelize) => {
+    await queryInterface.createTable('Users', {
+      id: { type: Sequelize.UUID, defaultValue: Sequelize.literal('(lower(hex(randomblob(16))))'), primaryKey: true },
+      name: { type: Sequelize.STRING, allowNull: false },
+      email: { type: Sequelize.STRING, allowNull: false, unique: true },
+      passwordHash: { type: Sequelize.STRING },
+      role: { type: Sequelize.ENUM('RENTER','HOST','ADMIN'), allowNull: false, defaultValue: 'RENTER' },
+      createdAt: { type: Sequelize.DATE, allowNull: false, defaultValue: Sequelize.literal('CURRENT_TIMESTAMP') },
+      updatedAt: { type: Sequelize.DATE, allowNull: false, defaultValue: Sequelize.literal('CURRENT_TIMESTAMP') }
+    });
+  },
+  down: async (queryInterface /*, Sequelize*/) => {
+    await queryInterface.dropTable('Users');
+  }
+};
